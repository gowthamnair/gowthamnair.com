---
title: "Skill-Set Breakdown"
layout: page
order: 4
icon: fa-snowflake-o
---
  
  ```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

This R Markdown document is made interactive using Shiny. Unlike the more traditional workflow of creating static reports, you can now create documents that allow your readers to change the assumptions underlying your analysis and see the results immediately. 

To learn more, see [Interactive Documents](http://rmarkdown.rstudio.com/authoring_shiny.html).

## Inputs and Outputs

You can embed Shiny inputs and outputs in your document. Outputs are automatically updated whenever inputs change.  This demonstrates how a standard R plot can be made interactive by wrapping it in the Shiny `renderPlot` function. The `selectInput` and `sliderInput` functions create the input widgets used to drive the plot.

```{r eruptions, echo=FALSE}

library(yaml)
library(RCurl)
library(knitr)
library(tidyverse)
library(igraph)
library(statnet)
library(ggplot2)
library(visNetwork)
library(igraph)
library(dplyr)
library(geomnet)
library(plotly)
library(shiny)
library(httr)
library(curl)
library(XML)





ui <- fluidPage(
  
  
  visNetworkOutput("network")
)


server <- function(input, output) {
  
  
  
  
  
  
  output$network <-   renderVisNetwork({
    
    
    
    
    
    x <- getURL("https://docs.google.com/spreadsheets/d/e/2PACX-1vSXzdijPm8IiuPm8TXk8w5lbPRLt7K4BNR3WTlkvAoLwPnZvqzHz40s6Vkv5toyR2-InkCpE2-8Hdc-/pub?output=csv")
    check <- read.csv(text = x)
    x <- getURL("https://docs.google.com/spreadsheets/d/e/2PACX-1vThRvBGZ9wty4hmOsK0-NDkMgvUgnrbxyFxmadtBTkS-BLxAczmRCu7weLvZ3il9NMpq6NKNbjVHG8A/pub?output=csv")
    nodesvalue <- read.csv(text = x)
    
    
    edges <- data.frame(from = check$SkillType, to = check$Skill, value = check$EdgeValue )
    nodes<- data.frame(id= nodesvalue$SkillType, value = nodesvalue$NodeValue , color = nodesvalue$NodeValue , group = nodesvalue$Group)
    
    
    
    
    
    visNetwork(nodes, edges, background = "white" ,) %>%
      visOptions(highlightNearest = TRUE,autoResize = TRUE) %>%
      visGroups(groupname = "1", color = rgb(0,0,0, 1) , shape="dot" , mass= 50 , value = 50) %>% 
      visGroups(groupname = "2", color = rgb(0.0,0.8,0.6, 0.8) , shape="dot" , mass= 0.8) %>%
      visGroups(groupname = "3", color = rgb(0.0,0.8,0.6, 0.6) , shape="dot" , mass= 0.8) %>%
      visEdges(arrows = "to", dashes = TRUE,smooth = TRUE ,shadow=TRUE, color = c(color=rgb(0,0,0, 0.3),highlight="orange",hover=rgb(1,0.60,0, 0.5))) %>%
      visNodes( shape="dot",font = list(color="black",background= "white", size= 20) ,shadow = TRUE ,   color= c(background=rgb(0.0,0.8,0.6, 0.9) ,border=rgb(0,0,0, 1),highlight="white" ),mass= 1.5 )%>%
      visInteraction(hover = TRUE) %>% 
      visInteraction(navigationButtons = TRUE)
  })
  
  
  
}
shinyApp(ui = ui, server = server)


```

```{r}

```


